
name: 'olist_analytics'
version: '1.0.0'
profile: 'olist_analytics'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table